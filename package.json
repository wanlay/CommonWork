{
  "name": "my-site",
  "version": "1.0.0",
  "description": "my-site",
  "main": "index.js",
  "scripts": {
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "precommit": "lint-staged",
    "lint": "prettier --write 'docs/**/*.md'"
  },
  "lint-staged": {
    "*.{js,json,css,md.vue}": [
      "prettier --write",
      "git add"
    ]
  },
  "author": "wanlay",
  "license": "ISC",
  "devDependencies": {
    "prettier": "1.13.7",
    "vuepress": "^0.10.0",
    "husky": "0.14.3",
    "lint-staged": "7.2.0"
  }
}